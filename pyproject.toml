[project]
name = "toronto-events"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "orjson>=3.11.5",
    "regex>=2026.1.15",
    "tqdm>=4.67.1",
]
