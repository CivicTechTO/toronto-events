# Processed outputs

This folder is intended for the final artifacts:
- `toronto_event_sources.csv`: ranked seed list of domains with Toronto/GTA metrics and flags
- `toronto_event_samples.ndjson`: sample normalized event rows per accepted domain (for downstream testing)
- `manual_review_queue.csv`: domains needing human review

